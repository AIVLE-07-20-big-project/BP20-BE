spring:
  config:
    import: optional:file:.env[.properties]
  # Docker Compose 자동 실행 해제
  docker:
    compose:
      enabled: false
  datasource:
    url: jdbc:mysql://localhost:${MYSQL_PORT:3306}/${MYSQL_DATABASE:bp20}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: aivle
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

kma:
  service-key: 89dc6a0054ee9c0cc56f127e002d03b2a67b62eff51753d2b30d58ef7b470700
  forecast-url: https://apis.data.go.kr/1360000/VilageFcstInfoService_2.0/getVilageFcst